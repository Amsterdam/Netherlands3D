using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gameobjectdatatest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GameObjectData god = new GameObjectData();
        //god.name = "parent";

        //GameObjectData child = new GameObjectData();
        //child.name = "child";
        //god.children = new List<GameObjectData>();
        //god.children.Add(child);

        //string json = JsonUtility.ToJson(god, true);
        //Debug.Log(json);

        //GameObjectData result = JsonUtility.FromJson<GameObjectData>(json);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
